const AnnouncementBar = () => (
  <div className="w-full bg-primary text-primary-foreground text-center text-xs font-medium tracking-button uppercase h-9 flex items-center justify-center">
    Free Worldwide Shipping on All Orders
  </div>
);

export default AnnouncementBar;
