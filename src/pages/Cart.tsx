import Layout from '@/components/layout/Layout';

const Cart = () => (
  <Layout>
    <section className="max-w-site mx-auto px-4 lg:px-8 py-20 text-center">
      <h1 className="font-heading text-3xl lg:text-5xl tracking-heading uppercase text-foreground mb-4">Cart</h1>
      <p className="text-muted-foreground">Cart page — coming soon</p>
    </section>
  </Layout>
);

export default Cart;
